version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: inventory-frontend
    ports:
      - "3000:80"
    networks:
      - inventory-network
    environment:
      - VITE_API_URL=http://localhost:3000

networks:
  inventory-network:
    driver: bridge
